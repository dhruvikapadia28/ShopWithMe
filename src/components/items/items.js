import { Fragment } from "react";
import AvailableItems from "./availableItems";

const Items = (props) => {

  return (
    <Fragment>
      <AvailableItems/>
    </Fragment>
  );
};

export default Items;
